import { StyleSheet, } from 'react-native';

const styles = StyleSheet.create({
    root: {
        flex: 1,
    },
    searchbox: {
        top: 0,
        flexDirection: 'row',
        minHeight: 40,
        width: '100%'
    },
    input:{
        width: '90%',
        color: '#90A4AE'
    }
});

export default styles;